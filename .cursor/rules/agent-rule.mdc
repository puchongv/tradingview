---
description: Universal Agent Rules - Core behavior for all projects
globs: ["**/*"]
alwaysApply: true
---

# Universal Agent Rules

## 🚨 CRITICAL: READ THIS FIRST
**ทุก Agent ที่ทำงานในโปรเจคนี้ ต้องอ่านไฟล์นี้ก่อนเสมอ!** (แก้ไขได้เฉพาะเมื่อได้รับอนุญาตเท่านั้น)
ไฟล์นี้คือ SOP หลักในการทำงานร่วมกับผู้ใช้งาน โดยจุดประสงค์หลักเพื่อที่จะให้ผู้ใช้งานสามารถทำงานกับ Agent ต่างเครื่องได้ โดยที่ Agent ใหม่สามารถเข้าใจ และ resume งาน รวมถึง conversion , project progress ได้โดยที่ผู้ใช้งานไม่ต้องมาอธิบายใหม่ เนื่องจากผู้ใช้งานทำงานด้วย computer หลายเครื่อง

โดยจะมีไฟล์ย่อยที่จะจัดเก็บบริบทต่างกันดังนี้
1. agent/agent-conversation-context.md (เก็บการสนทนาและ action ที่ได้กระทบกับผู้ใช้งาน)
2. agent/agent-project-requirements.md (เก็บรายละเอียดต่างๆ ของ โปรเจค เช่น Project Requirement , User Requirement , User Goal , Technical Details , Critical Point , Database TABLE DDL)
3. agent/project-server-connection.md (เก็บข้อมูล การเชื่อมต่อต่างๆ เช่น IP , PORT , login)

## 📁 FILE STRUCTURE GUIDELINES

### Folder Organization:
- **agent/**: for agent to understand all details of the project
- **agent-script/**: for agent create program , tools , script for process or analyst data for agent it self.
- **report/**: for agent to genarate a result of execution
- **Root**: for keep user struff (non agent)

## 📋 MANDATORY CHECKLIST
ก่อนเริ่มทำงาน ให้ทำตามลำดับนี้:

### 1. อ่านไฟล์ Context (บังคับ) หากพบไฟล์หลักเหล้านี้ ห้ามสร้างใหม่
```
1. อ่าน agent/agent-conversation-context.md 
2. อ่าน agent/agent-project-requirements.md 
3. อ่าน agent/project-server-connection.md 
```

### 2. ตรวจสอบสถานะปัจจุบัน
- ดู `git status` เพื่อดูสถานะ git
- ดู `ls -la agent/*.md` เพื่อดูไฟล์เอกสาร
- ตรวจสอบว่ามีไฟล์ context ครบหรือไม่

### 3. สร้างไฟล์ Context (ถ้าไม่มี) 
- สร้าง `agent/agent-conversation-context.md` 
- สร้าง `agent/agent-project-requirements.md` 
- สร้าง `agent/project-server-connection.md` 

** หากไม่พบฟายนี้อาจจะหมายความว่า เป็น Project ใหม่ ให้เริ่มต้นถามผู้ใช้งานว่ากำลังจะทำอะไร แล้วจึงอัพเดทข้อมูลต่างๆลงไฟล์อย่างละเอียดเพื่อให้ Agent คนใหม่เข้าใจ และต้องคอย อัฟเดทหากมีการเปลี่ยน requirement หรือ บริบท เมื่อโปรเจ็กมีการเปลี่ยนแปลง หากพบว่ามีการเปลี่ยน requirement สามารถลบ conversation ของ details ก่อนหน้าทิ้งได้

## 🔄 UNIVERSAL WORKFLOW RULES

### Before Starting Work:
1. **Read Context Files** (บังคับ)
2. **Check Git Status** - `git status`
3. **Sync with Remote** - `git pull origin main`
4. **Create Missing Context** - ถ้าไฟล์ context ไม่มี

### During Work:
1. **Update Context** - อัพเดทไฟล์ context ตามการเปลี่ยนแปลง
2. **Follow Project Focus** - ตาม project-requirements.md
3. **Document Changes** - บันทึกการเปลี่ยนแปลง
4. **Sync Regularly** - commit และ push บ่อยๆ

### After Work:
1. **Update Context Files** - อัพเดทสถานะ
2. **Commit Changes** - พร้อมข้อความชัดเจน
3. **Push to GitHub** - เพื่อ sync กับเครื่องอื่น
4. **Update Status** - ใน agent-conversation-context.md

## 📝 CONTEXT MANAGEMENT RULES

### Always Update These Files:
1. **agent-conversation-context.md** - เก็บการสนทนา, action log, user interaction
2. **project-requirements.md** - เก็บรายละเอียดโปรเจค, technical details, file structure
3. **agent-rule.mdc** - เก็บ core workflow และ rules (แก้ไขได้เฉพาะเมื่อได้รับอนุญาต)

### File Usage Guidelines:
- **agent-conversation-context.md**: ใช้สำหรับให้ agent ใหม่เข้าใจความเป็นไปทั้งหมด
- **project-requirements.md**: ใช้สำหรับรายละเอียดโปรเจคและ technical details
- **agent-rule.mdc**: ใช้สำหรับ core workflow และ rules (แก้ไขได้เฉพาะเมื่อได้รับอนุญาต)

### Update Format:
```markdown
## Last Update: [DATE]
## Status: [CURRENT_STATUS]
## Next Steps: [NEXT_ACTIONS]
```

## 🔄 GIT SYNC RULES

### Daily Sync:
1. **Pull Changes** - `git pull origin main`
2. **Check Status** - `git status`
3. **Resolve Conflicts** - ถ้ามี
4. **Push Changes** - `git push origin main`

### Before Major Work:
1. **Sync with Remote** - `git pull origin main`
2. **Check for Updates** - ดูการเปลี่ยนแปลงล่าสุด
3. **Update Context** - อัพเดทไฟล์ context

## 🚨 EMERGENCY RULES

### If Context is Lost:
1. **Read All .md Files** - เริ่มจาก agent-rule.mdc
2. **Check Git History** - `git log --oneline -10`
3. **Ask User** - "อ่าน context files แล้ว ต้องการทำอะไรต่อ?"
4. **Rebuild Context** - ถ้าจำเป็น

### If User is Confused:
1. **Show Current Status** - จาก context files
2. **Explain Project** - จาก project-requirements.md
3. **Show Progress** - จาก git history
4. **Ask for Direction** - ต้องการทำอะไรต่อ

## 📋 DAILY CHECKLIST

### Morning (First Work):
- [ ] Pull latest changes
- [ ] Read context files
- [ ] Check project status
- [ ] Plan day's work

### During Work:
- [ ] Update context files
- [ ] Document changes
- [ ] Sync with git
- [ ] Follow project focus

### End of Day:
- [ ] Update all context files
- [ ] Commit and push changes
- [ ] Update status
- [ ] Plan next steps

## 🎯 SUCCESS METRICS
- ✅ Context maintained across machines
- ✅ User can continue work seamlessly
- ✅ Project requirements clear
- ✅ Conversation history preserved

---

**Remember: Always read context files first and sync with git regularly!**